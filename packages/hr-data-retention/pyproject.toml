[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "orgmetra-hr-data-retention"
version = "0.1.0"
description = "Governed, value-minimized HR data retention review evidence for Orgmetra."
requires-python = ">=3.12"

[project.optional-dependencies]
test = ["pytest>=8.3", "pytest-cov>=5.0"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
  "--cov=orgmetra_hr_data_retention",
  "--cov-branch",
  "--cov-report=term-missing",
  "--cov-fail-under=100",
]
