# Changelog

## 0.1.0 - Unreleased

- Add value-minimized `OrganizationHierarchyChangeReviewPacket` for reviewed Organization Unit parent changes.
- Preserve separate business-effective and review-evidence time; validate fixed-offset `recorded_at` representation and reject future issuance values without claiming that the leaf package attests internal-clock provenance. Authoritative audit/outbox time remains owned by the mutation/persistence boundary.
- Support real root attach/detach transitions without sentinel parent identifiers.
- Require same-purpose controlled reason evidence, distinct accountable requester/reviewer correlations, deterministic canonical JSON/SHA-256, redacted representation, and post-construction tamper detection.
- Reject caller-defined packet classes at subclass creation before they can override validation or attribute-resolution hooks, preventing checked-vs-emitted evidence substitution and `__post_init__` bypass without retaining an unreachable runtime-type branch.
- Capture every trust-bearing field once for issuance, run all semantic validation and canonical creation sealing on that same snapshot, and derive the live-reference key from it; bind the exact canonical JSON and ordered field state into closure-private issuance evidence for later export verification.
- Make issuance single-use per live packet identity: atomically reject an object that is already issued or concurrently issuing so direct `__post_init__()` re-entry cannot reseal a low-level-retargeted packet under a fresh reference; release only failed first-issuance reservations so independent packet construction remains concurrent.
- Keep the mutable creation-digest, creation-canonical-payload, creation-state, in-progress issuance, weak live-reference and packet-retention registries plus their lock in closure-private packet runtime state rather than module globals, so ordinary module consumers receive no direct mutation capability over the in-process defense.
- Bind creation sealing, canonical export verification, and `sha256_digest()` to one SHA-256 implementation captured by the packet runtime, so replacing the later module-level digest binding cannot forge changed evidence into matching its creation seal.
- Bind the top-level issuance semantic validator to the packet runtime before packet construction, so later replacement of the module `_validate_issuance_snapshot` binding cannot bypass mandatory requester/reviewer separation or the other issuance semantic invariants before the creation seal is written; this remains a narrow module-binding defense rather than a general same-process sandbox.
- Bind the validator/canonicalizer authority selected by that runtime to its source-module dependencies and governed constants, so ordinary later reassignment of `_validate_reference`, `_validate_digest`, `_validate_code`, timestamp/type helpers, UUID/pattern bindings, or the fixed governance vocabulary cannot change the semantics of evidence already governed by the constructed packet runtime.
- Bind issuance snapshot selection, canonical payload construction, canonical serialization, and the still-live reference binding constructor to the packet runtime before packet construction, so later replacement of `_snapshot`, `_payload_from_snapshot`, `_canonical_payload_json`, or `_LiveReferenceBinding` cannot make validation/sealing observe different evidence or replace the live-reference digest conflict guard.
- Bind the package-root and implementation-module public builder paths to the governed packet constructor captured at package import, so later ordinary reassignment of the implementation-module `OrganizationHierarchyChangeReviewPacket` name cannot turn the builder into an unvalidated packet factory.
- Bind post-issuance export verification to one direct exact-slot snapshot and the closure-private issuance state/canonical payload, so replacing module `_payload` or serialization helpers with stale pre-mutation data cannot authorize a changed retained packet.
- Reject a post-issuance exact-`datetime` substitution carrying caller-defined `tzinfo` before datetime equality can invoke nested timezone behavior; issuance already guarantees and privately retains a built-in fixed-offset timestamp, so export checks only the mutable retained `recorded_at` value's nested timezone type before equality rather than adding an unreachable issuance-state branch.
- Reject replacement of issued `recorded_at` by a distinct exact built-in `datetime` object even when Python would consider the two aware datetimes equal because they represent the same instant under different fixed offsets; post-issuance datetime integrity is bound to the immutable object captured at issuance rather than instant-based equality.
- Bind each still-live tenant-qualified hierarchy-change reference to one evidence digest while permitting exact idempotent duplicates, preventing conflicting valid reissuance under the same review correlation.
- Normalize fixed-offset `recorded_at` values that cannot be represented as UTC to a governed validation error.
- Keep all packets fail-closed as human-review evidence only; authoritative same-tenant bitemporal re-resolution, stale-parent/cycle/multiple-parent checks, authoritative system time, and immutable audit/outbox remain required before mutation.
- Add exact installed-wheel CPython 3.14.7 quality gating with 100% owned statement and branch coverage through canonical Foundation CI.
