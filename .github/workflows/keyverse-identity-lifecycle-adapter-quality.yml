name: Keyverse Identity Lifecycle Adapter Quality

on:
  pull_request:
    branches:
      - bootstrap
      - develop
      - main
    paths:
      - "packages/keyverse-identity-lifecycle-adapter/**"
      - "docs/doctoring/keyverse-identity-lifecycle-references.md"
      - "docs/traceability/keyverse-identity-lifecycle.md"
      - ".github/requirements/foundation-test.txt"
      - ".github/workflows/keyverse-identity-lifecycle-adapter-quality.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: keyverse-identity-lifecycle-adapter-quality-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: Keyverse identity lifecycle contract and 100% coverage
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Prove exact candidate checkout
        env:
          ORGMETRA_EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$ORGMETRA_EXPECTED_HEAD_SHA"
      - name: Set up exact Python runtime
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14.7"
          check-latest: false
      - name: Install reviewed test and build toolchain
        run: |
          python -m pip install --require-hashes --no-deps --only-binary=:all: -r .github/requirements/foundation-test.txt
          printf '%s\n' 'setuptools==84.0.0 --hash=sha256:51a52592b3b99e102b609654876bd65f19f999935166d1352678931132b0c670' > /tmp/orgmetra-keyverse-build.txt
          python -m pip install --require-hashes --no-deps --only-binary=:all: -r /tmp/orgmetra-keyverse-build.txt
          python -m pip check
      - name: Compile adapter boundary
        run: python -m compileall -q packages/keyverse-identity-lifecycle-adapter/src packages/keyverse-identity-lifecycle-adapter/tests
      - name: Build and install exact package artifact
        run: |
          rm -rf /tmp/orgmetra-keyverse-build /tmp/orgmetra-keyverse-dist /tmp/orgmetra-keyverse-venv
          cp -a packages/keyverse-identity-lifecycle-adapter /tmp/orgmetra-keyverse-build
          mkdir -p /tmp/orgmetra-keyverse-dist
          python -m pip wheel --no-deps --no-build-isolation --wheel-dir /tmp/orgmetra-keyverse-dist /tmp/orgmetra-keyverse-build
          test "$(find /tmp/orgmetra-keyverse-dist -maxdepth 1 -type f -name '*.whl' | wc -l)" -eq 1
          python -m venv /tmp/orgmetra-keyverse-venv
          /tmp/orgmetra-keyverse-venv/bin/python -m pip install --require-hashes --no-deps --only-binary=:all: -r "$GITHUB_WORKSPACE/.github/requirements/foundation-test.txt"
          wheel_path="$(find /tmp/orgmetra-keyverse-dist -maxdepth 1 -type f -name '*.whl' -print -quit)"
          wheel_sha="$(sha256sum "$wheel_path" | awk '{print $1}')"
          printf 'orgmetra-keyverse-identity-lifecycle-adapter[test] @ file://%s --hash=sha256:%s\n' "$wheel_path" "$wheel_sha" > /tmp/orgmetra-keyverse-install.txt
          /tmp/orgmetra-keyverse-venv/bin/python -m pip install --require-hashes --no-deps -r /tmp/orgmetra-keyverse-install.txt
          /tmp/orgmetra-keyverse-venv/bin/python -m pip check
          /tmp/orgmetra-keyverse-venv/bin/python - <<'PY'
          from importlib.metadata import metadata
          from pathlib import Path
          import coverage
          import pytest
          import pytest_cov
          import orgmetra_keyverse_identity_lifecycle_adapter

          venv_root = Path("/tmp/orgmetra-keyverse-venv").resolve()
          module_path = Path(orgmetra_keyverse_identity_lifecycle_adapter.__file__).resolve()
          if not module_path.is_relative_to(venv_root):
              raise SystemExit(f"package imported outside isolated environment: {module_path}")
          for module in (coverage, pytest, pytest_cov):
              dependency_path = Path(module.__file__).resolve()
              if not dependency_path.is_relative_to(venv_root):
                  raise SystemExit(
                      f"test dependency imported outside isolated environment: {dependency_path}"
                  )
          if "test" not in (metadata("orgmetra-keyverse-identity-lifecycle-adapter").get_all("Provides-Extra") or []):
              raise SystemExit("built distribution does not expose the reviewed test extra")
          PY
      - name: Test installed Keyverse identity lifecycle artifact
        env:
          COVERAGE_FILE: /tmp/orgmetra-keyverse-identity-lifecycle.coverage
        run: |
          cd /tmp
          /tmp/orgmetra-keyverse-venv/bin/python -m pytest -c "$GITHUB_WORKSPACE/packages/keyverse-identity-lifecycle-adapter/pyproject.toml" "$GITHUB_WORKSPACE/packages/keyverse-identity-lifecycle-adapter/tests"
      - name: Require clean checkout
        run: |
          git diff --exit-code
          test -z "$(git status --porcelain)"
