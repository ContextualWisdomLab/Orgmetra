name: HR Data Disposition Quality

on:
  pull_request:
    branches:
      - develop
      - feat/hr-data-retention-review
    paths:
      - "packages/hr-data-disposition/**"
      - ".github/requirements/foundation-test.txt"
      - ".github/workflows/hr-data-disposition-quality.yml"
      - "docs/doctoring/hr-data-disposition-references.md"
      - "docs/traceability/hr-data-disposition-request.md"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hr-data-disposition-quality-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: Disposition request contract and 100% coverage
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Prove exact candidate checkout
        env:
          ORGMETRA_EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$ORGMETRA_EXPECTED_HEAD_SHA"
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14"
          check-latest: false
      - name: Install reviewed test toolchain
        run: |
          python -m pip install --require-hashes --no-deps --only-binary=:all: -r .github/requirements/foundation-test.txt
          python -m pip check
      - name: Compile disposition request package
        run: python -m compileall -q packages/hr-data-disposition/src packages/hr-data-disposition/tests
      - name: Test disposition request with exact statement and branch coverage
        env:
          PYTHONPATH: packages/hr-data-disposition/src
          COVERAGE_FILE: /tmp/orgmetra-hr-data-disposition.coverage
        run: python -m pytest -c packages/hr-data-disposition/pyproject.toml packages/hr-data-disposition/tests
      - name: Require clean checkout
        run: |
          git diff --exit-code
          test -z "$(git status --porcelain)"
