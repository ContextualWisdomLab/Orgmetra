name: Performance Context Evidence Quality

on:
  pull_request:
    branches:
      - develop
    paths:
      - "packages/performance-context-evidence/**"
      - ".github/requirements/foundation-test.txt"
      - ".github/requirements/performance-context-evidence-build.txt"
      - ".github/workflows/performance-context-evidence-quality.yml"
      - "docs/adr/0093-governed-performance-context-evidence.md"
      - "docs/doctoring/performance-context-evidence-references.md"
      - "docs/traceability/performance-context-evidence.md"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: performance-context-evidence-quality-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: Performance context evidence contract and 100% coverage
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Prove exact candidate checkout
        env:
          ORGMETRA_EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$ORGMETRA_EXPECTED_HEAD_SHA"
      - name: Set up exact Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14.7"
          check-latest: false
      - name: Install reviewed test and build toolchain
        run: |
          python -m pip install --require-hashes --no-deps --only-binary=:all: -r .github/requirements/foundation-test.txt
          python -m pip install --require-hashes --no-deps --only-binary=:all: -r .github/requirements/performance-context-evidence-build.txt
          python -m pip check
      - name: Compile package
        run: python -m compileall -q packages/performance-context-evidence/src packages/performance-context-evidence/tests
      - name: Build and install exact package artifact
        run: |
          rm -rf /tmp/orgmetra-performance-context-build /tmp/orgmetra-performance-context-dist /tmp/orgmetra-performance-context-venv
          cp -a packages/performance-context-evidence /tmp/orgmetra-performance-context-build
          mkdir -p /tmp/orgmetra-performance-context-dist
          python -m pip wheel --no-deps --no-build-isolation --wheel-dir /tmp/orgmetra-performance-context-dist /tmp/orgmetra-performance-context-build
          test "$(find /tmp/orgmetra-performance-context-dist -maxdepth 1 -type f -name '*.whl' | wc -l)" -eq 1
          python -m venv /tmp/orgmetra-performance-context-venv
          /tmp/orgmetra-performance-context-venv/bin/python -m pip install --require-hashes --no-deps --only-binary=:all: -r "$GITHUB_WORKSPACE/.github/requirements/foundation-test.txt"
          wheel_path="$(find /tmp/orgmetra-performance-context-dist -maxdepth 1 -type f -name '*.whl' -print -quit)"
          wheel_sha="$(sha256sum "$wheel_path" | awk '{print $1}')"
          printf 'orgmetra-performance-context-evidence @ file://%s --hash=sha256:%s\n' "$wheel_path" "$wheel_sha" > /tmp/orgmetra-performance-context-install.txt
          /tmp/orgmetra-performance-context-venv/bin/python -m pip install --require-hashes --no-deps -r /tmp/orgmetra-performance-context-install.txt
          /tmp/orgmetra-performance-context-venv/bin/python -m pip check
      - name: Test installed artifact with exact statement and branch coverage
        env:
          COVERAGE_FILE: /tmp/orgmetra-performance-context.coverage
        run: |
          cd /tmp
          /tmp/orgmetra-performance-context-venv/bin/python -m pytest -c "$GITHUB_WORKSPACE/packages/performance-context-evidence/pyproject.toml" "$GITHUB_WORKSPACE/packages/performance-context-evidence/tests"
      - name: Require clean checkout
        run: |
          git diff --exit-code
          test -z "$(git status --porcelain)"
