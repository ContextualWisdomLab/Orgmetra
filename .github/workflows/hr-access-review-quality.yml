name: HR Access Review Quality

on:
  pull_request:
    branches:
      - develop
    paths:
      - "packages/hr-access-review/**"
      - ".github/requirements/foundation-test.txt"
      - ".github/workflows/hr-access-review-quality.yml"
      - "docs/doctoring/hr-access-review-references.md"
      - "docs/traceability/hr-access-review.md"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hr-access-review-quality-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  unit:
    name: HR access review contract and 100% coverage
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Prove exact candidate checkout
        env:
          ORGMETRA_EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$ORGMETRA_EXPECTED_HEAD_SHA"
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.14"
          check-latest: false
      - name: Install reviewed test toolchain
        run: |
          python -m pip install --require-hashes --no-deps --only-binary=:all: -r .github/requirements/foundation-test.txt
          python -m pip check
      - name: Compile access review package
        run: python -m compileall -q packages/hr-access-review/src packages/hr-access-review/tests
      - name: Test access review with exact statement and branch coverage
        env:
          PYTHONPATH: packages/hr-access-review/src
          COVERAGE_FILE: /tmp/orgmetra-hr-access-review.coverage
        run: python -m pytest -c packages/hr-access-review/pyproject.toml packages/hr-access-review/tests
      - name: Require clean checkout
        run: |
          git diff --exit-code
          test -z "$(git status --porcelain)"
